# Session du 2026-09-16 (22:10) → 17/09 (00:45) — run 4.1b « Convertir un lead en contact et entreprise »

Session précédente : `2026-09-16-sync-4-1a.md`.

## État à la fermeture

- **Feature 4** « Le pipeline commercial » : « En revue ». **4.1b en PR #42**, auditée (0 bloquant, 3 importants traités), recettée (3 écrans, 2 défauts corrigés et revus), rapport posté en commentaire, description réécrite, mergeable, 5 points à relire, 8 décisions. Tâches CRM-95 à 98 « Terminée ».
- CI : fragment e2e 2 rouge sur `e2e/emails.spec.ts:145` — « demain » calculé en UTC, échoue de minuit à 2 h à Paris. Sans rapport avec 4.1b. **Relancer le job après 2 h** avant le merge, et ouvrir une tâche isolée (`/pilot fix`).
- Worktree `~/Desktop/crm-workday-14` (poste A), branche `feature/CRM-95-convertir-lead` en `46fe3e5`, poussée. Fichier non suivi `.pilot/pr/CRM-95/fenetre-conversion.jpg` (image prise pendant le chargement, écartée). Aucun serveur ne tourne.
- `.pilot/calibration.md` : la version de `main` non commitée est **commitée dans la PR #42** (avec la ligne de 4.1b). Au `sync`, jeter la copie locale de `main` (`git checkout -- .pilot/calibration.md`) avant de tirer.
- Rapports : `.pilot/recette/rapport-pr-42.md`, `corps-pr-42.md` (ignorés de git).

## Incidents

1. Producteur 1 arrêté à 150 tours (29 min) : PR ouverte sans CRM-96 à l'écran ni CRM-98.
2. Reprise par `SendMessage` en tâche de fond, puis **session arrêtée faute de mémoire** ; spec d'écran commitée non lancée. Reprise humaine, producteur neuf au premier plan (26 min).
3. Le `tsc` local du correcteur n'a pas vu une erreur de typage de son test (TS7034) : corrigée par le lead (`9c4fb51`).
4. Bac à sable : `cd` vers le worktree refusé même pour `node` ; passe visuelle lancée depuis le dépôt principal avec `--serveur "npm --prefix <worktree> run dev"`. `sips` et les pipes `| grep` demandent une permission. Une image se copie entre worktrees par `git hash-object -w` + `update-index --cacheinfo` + `checkout-index`.
5. Attente d'une PR : `gh pr view … -q` avec `error()` ne change pas le code de sortie de façon fiable ; `gh run watch <id> --exit-status` fonctionne au premier plan.

## Leçons

- Une livraison XL dépasse la limite de 150 tours du producteur (deux fois de suite) : découper les XL, ou prévoir d'office un second producteur neuf au premier plan plutôt qu'une reprise par message.
- Ne jamais reprendre un agent par `SendMessage` sur cette machine : il passe en tâche de fond et la session peut mourir pendant l'attente.
- Idiome candidat : un test qui calcule une date relative (« demain ») le fait sur le jour civil Europe/Paris, jamais par `Date.now() + 86 400 000` en UTC.

## Prochaine commande

Relancer le fragment e2e 2 après 2 h, merge humain de la PR #42, puis `/pilot sync` (feature 4 : 4.1 terminée, cadrer 4.2 « Opportunités »).
